# Kubernetes Cluster Setup Commands

## MASTER NODE COMMANDS

### 1. Update System

sudo apt-get update -y

### 2. Install Docker

sudo apt-get install docker.io -y

sudo systemctl enable docker
sudo systemctl restart docker

### 3. Install Kubernetes Packages

sudo apt-get install -y apt-transport-https ca-certificates curl gpg


sudo mkdir -p /etc/apt/keyrings

```bash
curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.29/deb/Release.key | \
sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
```

```bash
echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.29/deb/ /' | \
sudo tee /etc/apt/sources.list.d/kubernetes.list
```

```bash
sudo apt-get update -y
```

```bash
sudo apt-get install -y kubelet kubeadm kubectl
```

```bash
sudo apt-mark hold kubelet kubeadm kubectl
```

Check Versions:

```bash
kubeadm version
kubectl version --client
kubelet --version
```

---

### 4. Disable Swap

```bash
sudo swapoff -a
```

```bash
sudo sed -i '/ swap / s/^/#/' /etc/fstab
```

---

### 5. Enable Kernel Modules

```bash
sudo modprobe br_netfilter
```

Create sysctl config:

```bash
cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward = 1
EOF
```

Apply:

```bash
sudo sysctl --system
```

Verify:

```bash
cat /proc/sys/net/bridge/bridge-nf-call-iptables
```

Expected output:

```bash
1
```

---

### 6. Configure containerd

```bash
sudo mkdir -p /etc/containerd
```

```bash
containerd config default | sudo tee /etc/containerd/config.toml
```

```bash
sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml
```

Verify:

```bash
grep SystemdCgroup /etc/containerd/config.toml
```

Restart services:

```bash
sudo systemctl restart containerd
sudo systemctl restart kubelet
```

Enable kubelet:

```bash
sudo systemctl enable kubelet
```

---

### 7. Initialize Kubernetes Master

```bash
sudo kubeadm init --pod-network-cidr=192.168.0.0/16
```

---

### 8. Configure kubectl for Master User

```bash
mkdir -p $HOME/.kube
```

```bash
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
```

```bash
sudo chown $(id -u):$(id -g) $HOME/.kube/config
```

---

### 9. Install Calico Network Plugin

```bash
kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.28.0/manifests/calico.yaml --validate=false
```

---

### 10. Check Cluster Status

```bash
kubectl get nodes
```

```bash
kubectl get pods -A
```

---

### 11. Generate Worker Join Command

```bash
kubeadm token create --print-join-command
```

---

## SLAVE / WORKER NODE COMMANDS

### 1. Update System

```bash
sudo apt-get update -y
```

---

### 2. Install Docker

```bash
sudo apt-get install docker.io -y
```

```bash
sudo systemctl enable docker
sudo systemctl restart docker
```

Check Docker:

```bash
docker --version
```

---

### 3. Install Kubernetes Packages

```bash
sudo apt-get install -y apt-transport-https ca-certificates curl gpg
```

```bash
sudo mkdir -p /etc/apt/keyrings
```

```bash
curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.29/deb/Release.key | \
sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
```

```bash
echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.29/deb/ /' | \
sudo tee /etc/apt/sources.list.d/kubernetes.list
```

```bash
sudo apt-get update -y
```

```bash
sudo apt-get install -y kubelet kubeadm kubectl
```

```bash
sudo apt-mark hold kubelet kubeadm kubectl
```

---

### 4. Disable Swap

```bash
sudo swapoff -a
```

```bash
sudo sed -i '/ swap / s/^/#/' /etc/fstab
```

---

### 5. Enable Kernel Modules

```bash
sudo modprobe br_netfilter
```

Create sysctl config:

```bash
cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward = 1
EOF
```

Apply:

```bash
sudo sysctl --system
```

---

### 6. Configure containerd

```bash
sudo mkdir -p /etc/containerd
```

```bash
containerd config default | sudo tee /etc/containerd/config.toml
```

```bash
sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml
```

Restart services:

```bash
sudo systemctl restart containerd
sudo systemctl restart kubelet
```

Enable kubelet:

```bash
sudo systemctl enable kubelet
```

---

### 7. Join Worker Node to Cluster

Run the join command generated on master:

```bash
kubeadm join <MASTER-IP>:6443 --token <TOKEN> \
--discovery-token-ca-cert-hash sha256:<HASH>
```

Example:

```bash
kubeadm join 172.31.26.147:6443 --token tij06v.kb2vqjikgd2ruml8 --discovery-token-ca-cert-hash sha256:9f1422c5f3a2817b6ea9a4383d940c2993412f8f1565fba22d0863373dacd2d8
```

---

## VERIFY CLUSTER

Run on master node:

```bash
kubectl get nodes
```

```bash
kubectl get pods -A
```

---

## TEST DEPLOYMENT

Create nginx deployment:

```bash
kubectl create deployment nginx --image=nginx
```

Expose service:

```bash
kubectl expose deployment nginx --type=NodePort --port=80
```

Check resources:

```bash
kubectl get all
```
